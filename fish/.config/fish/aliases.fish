# Shorthands
alias g git
alias t task
alias pe path-extractor

# Emacs aliases
alias e  "emacsclient -c"
alias et "emacsclient -t"
function ee
  emacsclient -c . $argv &
end

# For when I type too fast
alias gti git
alias ipa "ip a"
