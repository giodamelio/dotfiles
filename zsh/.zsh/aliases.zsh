alias top=htop
alias n=ncmpcpp

# Make rm, cp and mv a bit safer
alias rm='rm -I'
alias cp='cp -i'
alias mv='mv -i'

# Show progress on copy
alias cpv="rsync -poghb --backup-dir=/tmp/rsync -e /dev/null --progress --"

